---
- name: Symlink dotfiles
  file:
    src: "{{ ansible_env.PWD }}/../dots/{{ item.dot }}"
    dest: "{{ ansible_env.HOME }}/.{{ item.dest }}"
    state: link
  with_items: linking_paths

- include: gsettings.yml
