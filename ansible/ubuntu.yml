---
- name: Manage edran\'s dotfiles (https://github.com/edran/dotfiles)
  hosts: localhost
  connection: local
  roles:
    - set_dotfiles
    - basic
    - dev_basic
    - dev_python
    # - dev_cpp
    - mopidy
