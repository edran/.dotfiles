* `wget http://apt.puppetlabs.com/puppetlabs-release-trusty.deb /tmp/puppetlabs-release-trusty.deb`
* `dpkg -i /tmp/puppetlabs-release-trusty.deb`
* `sudo apt-get update`
* `sudo apt-get install puppet git-core`
* `ln -s ~/.dotfiles/puppet /etc/puppet/`
